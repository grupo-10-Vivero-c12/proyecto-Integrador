<!DOCTYPE html>
<html lang="es">

<head>
    <%- include('../partials/head.ejs') %>
        <title>Inisiar Sesión</title>
</head>

<body>

    <%- include('../partials/header.ejs') %>

        <main class="login">
            <div class="rute">
                <a href="/home">Home</a>
                <span>/</span>
                <a href="/Mi cuenta">Mi cuenta</a>
                <span>/</span>
                <a href="/login">Inicio de sesión</a>
            </div>

            <h2>Inicio de sesión</h2>
            <div class="form-container">
                <form action="/users/login" method="POST">
                    <div class="form">

                        <div class="input-container">
                            <label for="email">Email</label>
                            <input type="email" id="email" name="email" placeholder="aaaaaaaaa@example.com">
                            <% if (locals.errors && errors.email) { %>
                                <small class="error-message">
                                    <%= errors.email.msg %>
                                </small>
                                <% } %>
                        </div>

                        <div class="input-container">
                            <label for="password">Contraseña</label>
                            <input type="password" id="password" name="password" placeholder="password">
                            <% if (locals.errors && errors.password) { %>
                                <small class="error-message">
                                    <%= errors.password.msg %>
                                </small>
                                <% } %>
                                    <% if (locals.errors && errors.custom && !(errors.password || errors.email)) { %>
                                        <small class="error-message">
                                            <%= errors.custom.msg %>
                                        </small>
                                        <% } %>
                        </div>

                        <div class="recover-password">
                            <a href="#">¿Olvidaste la Contraseña?</a>
                        </div>

                        <div class="button-submit">
                            <button type="submit">Iniciar</button>
                        </div>

                        <div class="create-count">
                            <a href="/users/register">¿No tenés una cuenta? Crear una</a>
                        </div>
                        <hr>
                    </div>
                </form>



                <div class="log-in-google">
                    <a href="#"><img src="/images/logo-google.png" alt="google"> Crear cuenta con Google</a>
                </div>
            </div>
        </main>
        <%- include('../partials/footer.ejs') %>
</body>

</html>