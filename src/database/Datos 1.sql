use vivero_timbo;

INSERT INTO roles (name)
value ("ADMIN"),("USER");

INSERT INTO categories (name)
value ("Macetas"), ("Semillas"), ("Plantas");

INSERT INTO description 
value (default, default, default, default, default);
